<header>
	<nav class="navbar navbar-silk-way">
		<div class="container-fluid">
			<div class="row">
				<!-- Brand and toggle get grouped for better mobile display -->
				<div class="navbar-header">
					<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
						<span class="sr-only">Toggle navigation</span>
						<span class="icon-bar"></span>
						<span class="icon-bar"></span>
						<span class="icon-bar"></span>
					</button>
					{% if logo %}
						<a href="{{ home }}"><img src="{{ logo }}" title="{{ name }}" alt="{{ name }}" class="img-responsive" /></a>
					{% else %}
						<h1><a href="{{ home }}">{{ name }}</a></h1>
					{% endif %}
				</div>

				<!-- Collect the nav links, forms, and other content for toggling -->
				<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
					{{ position2 }}
					<div class="navbar-form navbar-left">{{ search }}</div>
					<ul class="nav navbar-nav navbar-right">
						<li id="top-links" class="nav header-dropdown">
							<ul class="list-inline">
								<li class="dropdown">
									<a href="{{ account }}" title="{{ text_account }}" class="dropdown-toggle" data-toggle="dropdown">
										<span class="hidden-xs">{{ text_account }}
									</a>
									<ul class="dropdown-menu dropdown-menu-right pt-account">
										{% if logged %}
											<li><a href="{{ account }}">{{ text_account }}</a></li>
											<li><a href="{{ order }}">{{ text_order }}</a></li>
											<li><a href="{{ transaction }}">{{ text_transaction }}</a></li>
											<li><a href="{{ download }}">{{ text_download }}</a></li>
											<li><a id="pt-logout-link" href="{{ logout }}">{{ text_logout }}</a></li>
										{% else %}
											<li><a id="pt-register-link" href="{{ register }}">{{ text_register }}</a></li>
											<li><a id="pt-login-link" href="{{ login }}">{{ text_login }}</a></li>
										{% endif %}
									</ul>
								</li>
							</ul>
						</li>
						<a href="{{ home }}index.php?route=account/order" class="btn btn-default btn-lg">{{ text_track_shipment }}</a>
					</ul>
				</div><!-- /.navbar-collapse -->
			</div>
		</div><!-- /.container-fluid -->
	</nav>
	<div class="container header-body">
		<div class="row">
			<h1>
				доставка товара и сборных грузов
				<br>
				из Китая под ключ
				<br>
				В любой город россии
			</h1>
			<p>
				Мы предоставляем полный комплекс услуг по выкупу товаров непосредственно у производителей и поставщиков в Китае,
				<br>
				обеспечивая эффективное управление вашими закупками от начала и до конца.
				<br>
				Работая с нами, вы получаете полноценного представителя на территории КНР.
				<br>
				С нами вы не только выгодно приобретете товар, но и сможете заработать!
			</p>
		</div>
	</div>
</header>
